export const dynamic = "force-dynamic";

export default function PostJobPage() {
  return (
    <main style={{ padding: "2rem" }}>
      <h1>Post a Job</h1>
      <p>This page is rendered dynamically to avoid build-time errors.</p>
    </main>
  );
}
